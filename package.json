{
  "name": "dlsu-student-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "check": "node --check script.js && node --check dashboard.js && node --check api/auth/login.js && node --check api/dashboard.js",
    "create-user": "node scripts/create-user.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "bcryptjs": "^3.0.2",
    "jose": "^6.1.0"
  }
}
